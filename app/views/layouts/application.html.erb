<!DOCTYPE html>
<html>
  <head>
    <title>Manq</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>

<!-- NAV BAR -->
    <div class="ui menu">
      <div class="header item">
        <a href="/">ManQ</a>
      </div>
      <a class="item" href='/'>Home</a>
      <div class="right menu">
        <% if @current_user %>
        <!-- STORE -->
          <% if @current_user.user_type_id == 3 %>
            <div class="ui right dropdown item" id="dropdown-menu-store_manager">
              <span><%= @current_user.store.store_name %></span>
              <i class="dropdown icon"></i>
              <div class="menu">
                <div class="item"><a href="/stores/<%= @current_user.store.id %>">Store Profile</a></div>
                <div class="divider"></div>
                <div class="item"><%= link_to "Log Out", '/logout', method: :delete %></div>
              </div>
            </div>
            <!-- BIZ USER -->
          <% elsif @current_user.user_type_id == 2 %>
            <div class="ui right dropdown item" id="dropdown-menu-biz_user">
              <span><%= @current_user.biz_user.company_name %></span>
              <i class="dropdown icon"></i>
              <div class="menu">
                <div class="item"><a href="/stores">Stores Profile</a></div>
                <div class="divider"></div>
                <div class="item"><%= link_to "Log Out", '/logout', method: :delete %></div>
              </div>
            </div>
            <!-- ADMIN -->
          <% elsif @current_user.user_type_id == 1 %>
            <div class="ui right dropdown item" id="dropdown-menu-admin">
              <span>Admin</span>
              <i class="dropdown icon"></i>
              <div class="menu">
                <div class="item"><a href="/biz_users">Business Users</a></div>
                <div class="item"><a href="/stores">Stores</a></div>
                <div class="divider"></div>
                <div class="item"><%= link_to "Log Out", '/logout', method: :delete %></div>
              </div>
            </div>
          <% end %>
        <% else %>
        <a class="item" id='check-status'>Check Status</a>
        <a class="item" href="/biz_users/new">Register</a>
        <a class="item" id='manq-login-submit'>Sign In</a>
        <% end %>
      </div>
    </div>

<!-- LOG IN MODAL: UNHIDDEN BY JAVASCRIPT -->
    <div class="ui modal" id='login-modal'>
      <i class="close icon"></i>
      <div class="header">
        Sign In
      </div>
      <div class="content">
        <form class="ui form" action='/login' method='post'>
          <div class="field">
            <label>Email</label>
            <input type="text" name="registered_user[email]" placeholder="Email">
          </div>
          <div class="field">
            <label>Password</label>
            <input type="password" name="registered_user[password]" placeholder="Enter password here">
          </div>
      </div>
      <div class="actions">
        <button class="ui button" type="submit">Submit</button>
        </form>
      </div>
    </div>

<!-- CHECK QUEUE STATUS MODAL: UNHIDDEN BY JAVASCRIPT -->
<div class="ui basic small coupled modal" id="check-status-modal">
  <i class="close icon"></i>
  <div class="header">
    Check Queue Status
  </div>
  <div class="content">
    <form class="ui form">
    <div class="field">
      <label>Mobile Number</label>
      <input type="text" name="user[phone_no]" placeholder="Please enter the number you used earlier">
    </div>
  </div>
  <div class="actions">
    <button class="ui button" type="submit">Submit</button>
    </form>
  </div>
</div>

<div class="ui basic small coupled modal" id="return-status-modal">
  <i class="close icon"></i>
  <div class="header">
    Queue Status
  </div>
  <div class="content">
    <div class="field">
      show the queue status here
    </div>
  </div>
  <div class="actions">

  </div>
</div>

<!-- RENDER LAYOUT INSERTS -->
    <%= yield %>
  </body>
</html>
